{
  "name": "guessit-backend",
  "version": "0.0.0",
  "description": "",
  "main": false,
  "scripts": {
    "watch": "gulp watch",
    "compile": "gulp compile",
    "start": "nodemon -w ./dist ./app.js",
    "test": "nodemon -w ./dist ./dist/test/test.js",
    "make-frontend": "cd frontend && npm install && gulp compile && cp -R index.html config.js dist jspm_packages ../public"
  },
  "author": "ferrugemjs",
  "license": "ISC",
  "dependencies": {
    "event-emitter-lite": "^1.0.5",
    "express": "^4.14.0",
    "socket.io": "^1.4.8"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0",
    "gulp-rename": "^1.2.2",
    "gulp-replace": "^0.5.4",
    "gulp-typescript": "^2.11.0",
    "gulp-uglify": "^1.5.2",
    "nodemon": "^1.10.2",
    "require-dir": "^0.3.0"
  }
}
